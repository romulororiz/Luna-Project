// let baseUrl = '';
// if (process.env.NODE_ENV === 'development') {
// 	baseUrl = 'http://localhost:8000';
// } else {
const baseUrl = 'https://capricorn-luna.propulsion-learn.ch';
// }

export default baseUrl;
